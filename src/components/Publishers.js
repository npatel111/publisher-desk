import React from 'react'

export default function Publishers(props) {
  return (
    <div>
      <h1>Samples</h1>
      <p>I have never used HTML5/CSS3 before, just normal HTML and CSS, but I played around with it this weekend to learn what I would be dealing with. Here are some samples</p>
    </div>
  )
}
